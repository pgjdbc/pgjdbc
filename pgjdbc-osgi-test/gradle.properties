# pax-exam uses Maven dependency resolution, so it needs a repository
osgi.test.mavencentral.url=https://repo1.maven.org/maven2

# Do not publish the jar
# It adds little value, however it requires effort to maintain license clearance
nexus.publish=false
