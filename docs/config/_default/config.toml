# Production BaseURL
#baseURL = "https://pgjdbc.github.io/pgjdbcdocs/"
baseURL = "https://jdbc.postgresql.org/"
languageCode = "en-us"
title = "pgJDBC"
[permalinks]
page = "/:slug/"
posts = "/:year/:month/:slug/"
#changelogs = "/changelogs/:filename/"

# Search index for lunr.js
[outputFormats.SearchIndex]
baseName = "search"
mediaType = "application/json"

[outputs]
home = ["HTML", "RSS", "SearchIndex"]
# section = ["HTML", "RSS", "SearchIndex"]
page = ["HTML"]

# hugo.Data, used by layouts/partials/anchor-moves.html, was added in Hugo 0.156.0.
[module.hugoVersion]
min = "0.156.0"
